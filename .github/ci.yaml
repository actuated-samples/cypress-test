name: Chrome
on: push

jobs:
  chrome:
    runs-on: actuated
    name: E2E on Chrome
    steps:
      - uses: actions/checkout@v3
      - uses: cypress-io/github-action@v5
        with:
          browser: chrome
